---
name: yijing-divination
description: "本地易经占卜（梅花易数为主，六爻纳甲进阶）——有时间必跑 meihua_time.py 年月日时起卦；否则铜钱/数字法。禁止心算起卦。支持体用生克、卦辞爻辞、纳甲装卦(liuyao_pan.py)。当用户提到占卜、算卦、起卦、解卦、周易、六爻、梅花易数、问卦、测一卦时触发。"

---

# 本地易经占卜

纯本地运行的易经占卜技能。不调任何外部 API，隐私零风险。

> **体系定位**：以**梅花易数**（体用生克、万物类象、时间起卦）为默认路径；用户要世应/六亲/用神时走**六爻纳甲**（须跑 `liuyao_pan.py`）。

---

## ⚠️ Agent 起卦强制路由（先读此节，再执行）

**起卦前必须停读下表，选定方法并运行对应脚本。禁止 Agent 心算年月日时；禁止背固定年数。**

| 用户输入 | 必跑脚本 | 禁止 |
|----------|----------|------|
| 给出**具体日期/时间** | `scripts/meihua_time.py` | 心算；公历年份当年数 |
| 要**六爻/世应/六亲/用神** | 见下「六爻脚本链」 | 跳过起卦直接装卦 |
| **铜钱法** | `scripts/tongqian.py --json` | — |
| **数字法**（三个数） | `scripts/tongqian.py --nums A B C --json` | 心算互卦/体用 |
| 随口报三个数（旧说法） | 同上 `--nums` | — |

**Python 命令（Windows 优先 `py -3`，其次 `python`）**：

```bash
py -3 scripts/meihua_time.py 2026-07-04 06:00 --json
py -3 scripts/tongqian.py --json
py -3 scripts/tongqian.py --nums 105 372 891 --json
py -3 scripts/liuyao_pan.py --hex 地雷复 --moving 6 --date 2026-07-04 --topic 赛事 --json
py -3 scripts/ganzhi.py --json 2026-07-04
py -3 scripts/jieqi.py --json 2026-07-04 06:00
```

> **年数 ≠ 公历年份**：年数取农历年地支（子1…亥12），由脚本按时刻自动计算，随年份变动。

### 六爻脚本链（必按顺序）

1. **起卦**（二选一）  
   - 有时间：`py -3 scripts/meihua_time.py … --json`  
   - 无时间：`py -3 scripts/tongqian.py --json` 或 `--nums A B C --json`
2. **装卦**：从 JSON 取 `本卦.name`（或 `本卦.name` 字段）、`动爻` 列表 →  
   `py -3 scripts/liuyao_pan.py --hex 卦名 --moving 1,6 --date YYYY-MM-DD --topic 问事 --json`  
   （`liuyao_pan` 内已调 `ganzhi`/`jieqi` 月建，一般不必再单独跑 `ganzhi.py`）
3. **细断**：读 `references/bushizhengzong-rules.md`、`fenlei-zhangu.md` 等

> **体用禁止混用**：时间起卦**只**用 `meihua_time.py` 的「体用」；铜钱/数字法**只**用 `tongqian.py` JSON 的「体用」。不得用时间体用规则解铜钱卦。

### 联占三式（跨 skill 脚本路径）

本 skill 目录下**无**六壬/奇门脚本；联占时切换至兄弟 skill 并运行：

| 体系 | 脚本路径（相对本 skill 根目录） |
|------|--------------------------------|
| 大六壬 | `../daliuren-divination/scripts/daliuren.py --json YYYY-MM-DD HH:MM` |
| 奇门 | `../qimen-dunjia/scripts/qimen.py --json YYYY-MM-DD HH:MM` |

或使用各 skill 内 `daliuren_pan.py` / `qimen_pan.py`（支持 `--datetime`）。

### 三端联调抽检（Cursor / Claude / Hanako）

三端通过 junction 挂载**同一** `skills/divination-skills/` 源码。脚本层自动验：

```powershell
py -3 skills/divination-skills/verify-three-endpoints.py
```

**标准 UI prompt**（复制到三端各测一遍，确认 Agent 跑脚本且卦象一致）：

> 请用梅花易数时间起卦：公历 2026-07-04 06:00，问「今日运势如何」。要求：必须先运行 `meihua_time.py`（禁止心算）；解读前先给 6 行摘要。

**锚点期望**：本卦地雷复 · 年数 7 · 变卦山雷颐 · 体克用。

## 参考文件索引

本技能附带多个参考文件，按需读取：

| 文件                                  | 内容                                                         | 何时读取                                                     |
| ------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `references/hexagrams.md`             | 六十四卦速查表（卦名、上下卦、二进制、卦辞、大象传） + 八卦五行表 + 动爻规则 + 体用生克详解 | **每次起卦必读**，用于识别卦象和基础解读                     |
| `references/zhouyi-yaoci.md`          | 周易六十四卦爻辞速查（384 爻完整爻辞）                       | 需要引用特定爻辞时读取，按卦序+爻位定位                      |
| `references/meihua-wanwu-leixiang.md` | 梅花易数八卦万物属类（每卦 20+ 维度的物象表）                | 需要物品/人物/方位/颜色等具体类象判断时读取                  |
| `references/meihua-qigua.md`          | 梅花易数专属起卦法（年月日时、物数、声音、字占、方所、颜色、动物、静物占） | 用户指定用某方法或场景适合特殊起卦法时读取                   |
| `references/najia-bagong.md`          | 纳甲八宫基础（八宫卦序、装世应、纳地支、装六亲、装六神、用神取用） | 用户需要专业六爻层面的解读（如问具体某六亲、看世应关系）时读取 |
| `references/fenlei-zhangu.md`         | 分类占断指南（婚姻/求财/求名/疾病/官讼/出行/失物/行人/家宅/生产 等的断法） | 用户问及具体领域时读取，提供针对性的断法规则                 |
| `references/bushizhengzong-rules.md`  | 《卜筮正宗》核心规则（用神分类、世应论、原忌仇神、旺衰判断、旬空、暗动日破、反吟伏吟、进退神、飞伏神、卦身） | 需要系统性的六爻规则时读取，特别是用神取用和吉凶判断 |
| `references/liuyao-cases.md`          | 六爻实战案例集（求财、疾病、功名、婚姻、出行、失物、官司等案例） | 需要参考实际案例时读取，帮助理解断卦的实际应用 |

此外，工作空间可选 **《易经》完整 PDF**（`skills/卦书/易经-古籍典藏-luckclub.pdf`，151 页），包含全部 64 卦的卦辞、384 爻辞、彖传、象传、文言传及白话译文。爻辞已整理入 `references/zhouyi-yaoci.md`，彖传和文言传详情可查 PDF。

---

## 起卦前判断：不动不占，不因事不占

邵雍《梅花易数》开篇立规：「不动不占，不因事不占。」这句话是占卜的第一门槛，必须在起卦前完成判断。

**核心原则**：卦是跟着事来的，不是反过来。没有具体的事、真实的疑问，卦象空泛无意义。

### 什么算「具体的事」

判断标准三条，满足**至少一条**即可起卦：

1. **有具体选项**：你正在两个或多个选择之间犹豫（「两个 offer 选哪个」）
2. **有明确时间节点**：结果在可预见的未来会揭晓（「下个月的面试能不能过」）
3. **有真实未知**：事情已经发生或正在发生，但你对走向不确定（「对方这个态度是什么意思」）

**合格例子**：

- 「投了这家公司，面试完了在等结果，能拿到 offer 吗」
- 「最近和伴侣总因为钱吵架，该不该分开一段时间」
- 「下个月有个考试，复习状态不太好，怎么办」

### 什么不算「具体的事」

以下情况**不起卦，先引导用户聚焦**：

- 「帮我看看今年运势」—— 太泛，没有锚点
- 「我以后能不能发财」—— 时间跨度过大，没法映射到具体选择
- 「测一下我的命」—— 终身占不是梅花易数的用法
- 「随便算一卦看看」—— 没有因由，卦象无意义

### 引导话术

当用户的问题太泛时，用温和的方式帮他对焦：

> 「邵雍说『不动不占，不因事不占』—— 得有件具体的事，卦才解得准。能不能说说最近在纠结什么？比如某段关系、某个决定、某个卡住的节点？」

**不要**追问太多细节（会让人觉得烦），**不要**在完全没上下文时硬算（解读会空泛）。用户给出至少一处具体锚点，即可起卦。

如果用户的问题本身就合格（如「这个 offer 能不能拿到」「要不要跟那个人表白」），直接跳过引导，立刻起卦。

---

## 占卜规范

### 道德原则

《卜筮正宗》云：「筮必诚心——圣人作易，幽赞神明，以其道合乾坤故也。」占卜者当守以下规范：

1. **不过度依赖**：占卜是参考，不是人生的GPS导航。不能每顿饭吃什么都要算一卦
2. **不替代专业决策**：医疗、法律、财务问题应咨询专业人士，占卜仅为辅助参考
3. **不恐吓用户**：重在分析趋势和提供化解方向，不说「大凶」「必死」「血光之灾」
4. **尊重隐私**：占卜内容保密，不外泄
5. **诚心专一**：「今人求卜多有科头跣足，短衫露体，甚至有不焚香不洗手者，更有富贵自骄，差家人代卜，或烦亲友代卜，孰不知自虽发心而代者未必心虔？」（《卜筮正宗》）

### 禁忌原则

#### 不占之事

- **不占生死寿数**：「我什么时候会死」「我能活多久」——此类问题不占
- **不占已知结果**：明知故问、试探卦师——无诚意则卦不验
- **不占损人害己**：如何害人、如何违法——违道德伦理之事不占
- **不占戏谑玩闹**：「随便算一卦看看」——无具体事由则卦象空泛

#### 不占之时

- **心不诚时不占**：心存疑虑、半信半疑——卦象不准
- **酒后失神时不占**：神志不清则无法诚心感应
- **一日一事反复占**：说明内心已有答案，只是不想接受——《增删卜易》云：「再三则渎，渎则不告」

#### 解读禁忌

- **不说绝对化措辞**：「必死」「必败」「必成」——易经讲变化趋势，不测必然
- **不用恐吓性语言**：「血光之灾」「家破人亡」——即使卦象不吉，也要指出化解方向
- **不替用户做决定**：「你必须这样做」「你不能那样做」——提供分析，决定权在用户
- **不泄露天机过度**：点到为止，「天机不可尽泄」

---

## 起卦方法

> 路由表已前置至文首「Agent 起卦强制路由」。以下为各法细则。

### 年月日时起卦（有时间必用）

用户给出公历或本地时刻时，**必须**运行脚本（不可 Agent 心算）：

```bash
py -3 scripts/meihua_time.py 2026-07-04 06:00
py -3 scripts/meihua_time.py --datetime "2026-07-04 06:00" --json
py -3 scripts/meihua_time.py --json
```

首次使用需安装依赖：

```bash
pip install -r scripts/requirements.txt
```

含 **zhdate**（农历）、**ephem**（精确节气/月建）。未装 ephem 时 `ganzhi.py` 月建回退为近似日期表（见输出 `月建精度`）。

**唯一算法**（与 `references/meihua-qigua.md` 一致，禁止其它取法）：

| 项 | 取法 |
|----|------|
| 年数 | 农历年**地支**序数：子1、丑2 … 亥12（例：丙午年→午→**7**；**不是**公历 2026） |
| 月数 | 农历月：正月1 … 腊月12 |
| 日数 | 农历日：初一1 … 三十30 |
| 时数 | 时辰地支：子1 … 亥12（按时刻换算，如 06:00→卯时→4） |
| 上卦 | (年+月+日) ÷ 8 的余数 → 卦数（**余 0=坤**） |
| 下卦 | (年+月+日+时) ÷ 8 的余数 → 卦数（**余 0=坤**） |
| 动爻 | (年+月+日+时) ÷ 6 的余数 → 爻位（**余 0=上爻**） |
| 卦数 | 乾1、兑2、离3、震4、巽5、坎6、艮7、坤8 |

脚本输出含：农历换算、四数取值说明、计算过程、本卦/变卦/动爻/体用。**解读前将脚本输出原样或摘要展示给用户**，便于对账。

无 Python 环境时：请用户改报三个三位数走数字法，或请用户在本地运行脚本后把 JSON 结果贴回；**仍禁止 Agent 心算年月日时起卦**。

### 铜钱法步骤

使用 `scripts/tongqian.py` 模拟三枚铜钱投掷六次（从初爻到上爻）：

```bash
py -3 scripts/tongqian.py
py -3 scripts/tongqian.py --json
# 输出六行，每行为 6/7/8/9 之一，从初爻到上爻
```

**结果对照表**：

| 值 | 含义 | 阴阳 | 动/静 |
|----|------|------|--------|
| 6 | 老阴 | 阴 | **动爻**（变阳） |
| 7 | 少阳 | 阳 | 静爻 |
| 8 | 少阴 | 阴 | 静爻 |
| 9 | 老阳 | 阳 | **动爻**（变阴） |

> **Windows 用户注意**：优先 `py -3`；若无 Python，改走数字法或请用户本地跑脚本贴 JSON。

### 数字法（备选）

用户随口报**三个三位数**时使用（**本 skill 取法**，与部分教材「先上后下」不同，须按此执行）：

- 第一个数 ÷ 8 的余数 → **下卦**（0=坤，1=乾，2=兑，3=离，4=震，5=巽，6=坎，7=艮）
- 第二个数 ÷ 8 的余数 → **上卦**
- 第三个数 ÷ 6 的余数 → 动爻位置（0=上爻/第六爻）

> **提示**：请报三个完整的数字（如 105、372、891）。**不要报 0**。若用户习惯「第一数上卦、第二数下卦」，须先确认或改报数顺序。解读前须说明：**本 skill 第一数=下卦、第二数=上卦**。

### 梅花专项起卦（物数、字占、方所等）

`references/meihua-qigua.md` 有物数、声音、字占、方所等法，**当前无对应脚本**。

- **有具体时刻** → 改走 `meihua_time.py`
- **可报三个数** → 改走 `tongqian.py --nums`
- **否则** → 引导用户补充时间或报数；**禁止 Agent 心算**专项起卦

---

## 卦象识别

读取 `references/hexagrams.md` 获取完整六十四卦速查表和八卦对应表。

### 识别步骤

1. **定本卦**：六爻从下到上排列，下三爻为下卦（内卦），上三爻为上卦（外卦），查表得卦名和卦序
2. **识动爻**：记录所有动爻位置（值为 6 或 9 的爻位）。6 在本卦中为阴(0)，9 在本卦中为阳(1)
3. **定变卦（之卦）**：将动爻阴阳反转（6→阳，9→阴），静爻保持，得到新六爻，查表得变卦卦名
4. **若六爻均静（无 6 无 9）**：无变卦，只解本卦卦辞

### 二进制编码约定

**6 位，从左到右 = 从初爻（下）到上爻（上）。前 3 位为下卦，后 3 位为上卦。**
`1` = 阳爻，`0` = 阴爻。老阴 6 在本卦中视为阴(0)，老阳 9 在本卦中视为阳(1)。

### 体用定法（解卦必遵）

| 起卦方式 | 体用来源 |
|----------|----------|
| **年月日时**（`meihua_time.py`） | **以脚本 JSON/输出中的「体用」为准**，Agent 不得另算 |
| 铜钱法 / 数字法 | 见 `references/hexagrams.md`「体用生克详解」全文，摘要如下 |

**梅花体用摘要**（铜钱/数字法必遵）：

1. 有动爻的**经卦**（上卦或下卦）为**用**，无动爻的经卦为**体**
2. **上下经卦均有动爻**：动爻**较多**者为用；动爻数**相同**则**上卦**为用
3. **六爻皆静**：上卦为体，下卦为用

> **铜钱/数字法**：优先用 `tongqian.py --json` 输出的「体用」字段；若无 JSON 再按上表手推。

体用定好后，生克与吉凶见 `hexagrams.md` 体用生克表 + 卦辞/爻辞。

---

## 解读规则

### 解卦路径选择（闸门）

| 用户需求 / 问事 | 体系 | 必跑脚本 | 使用参考 |
|-----------------|------|----------|----------|
| 趋势、吉凶、要不要做 | **梅花** | 起卦脚本 | hexagrams.md、fenlei-zhangu.md |
| 卦辞爻辞、具体含义 | **梅花** | 起卦脚本 | + zhouyi-yaoci.md |
| 人物/物品/方位类象 | **梅花** | 起卦脚本 | + meihua-wanwu-leixiang.md |
| 世应、六亲、用神、应期 | **六爻** | **六爻脚本链**（起卦→`liuyao_pan.py`） | najia-bagong.md、bushizhengzong-rules.md |
| 赛事对阵（谁胜谁） | **六爻**（世应）或梅花 | 同上 | 见下「赛事六亲」 |

**赛事六亲**（六爻路径，世=己方、应=对方）：

| 六亲 | 赛事含义 |
|------|----------|
| 世爻 | 己方（如主队/你支持的一方） |
| 应爻 | 对方 |
| 子孙 | 进球、攻势、喜悦 |
| 妻财 | 收益、观众、盘口 |
| 官鬼 | 胜负关键、裁判、压力 |
| 父母 | 场地、赛事组织、规则 |

**六爻断卦七步**（走六爻路径时须按序核对，不可跳步）：

1. **装卦**：`liuyao_pan.py` → 八宫、世应、纳甲、六亲、六神、旬空、旺衰、**月破、入墓、化空/化墓、伏神、进退、卦身**
2. **定用神**：`--topic` → 用神 + **原神/忌神/仇神**；细则读 `bushizhengzong-rules.md`
3. **看旺衰**：每爻「旺衰」+「暗动日破」
4. **看动变**：变卦六亲、进退、反吟伏吟（纳甲地支）
5. **看空破**：旬空、**月破**、日破/暗动、化空
6. **断吉凶**：原/忌/仇、世应生克
7. **定应期**：出空、填实、逢合冲；配合 `ganzhi.py` / `jieqi.py`

**禁止**：走六爻路径却不跑 `liuyao_pan.py`；用体用生克代替妻财/官鬼等专业用神断法。

### 动爻解读优先级

按动爻数量确定解读重点（详见 `references/hexagrams.md`）：

1. **无动爻（六爻皆静）**：以本卦卦辞、大象传为主
2. **一爻动**：以本卦该动爻的爻辞为主判断
3. **二爻动**：以两动爻爻辞为主，位置靠上的动爻优先
4. **三爻动**：本卦卦辞 + 变卦卦辞综合判断
5. **四爻动**：以变卦中两个静爻的爻辞为主，靠下的静爻优先
6. **五爻动**：以变卦中唯一静爻的爻辞为主
7. **六爻全动**：以变卦卦辞为主。乾六爻全动用「用九：见群龙无首，吉」；坤六爻全动用「用六：利永贞」
8. **自报卦象**：用户已知卦名，直接按卦辞解

### 应期判断

当用户问「什么时候」「多久后」时，需要进行应期判断。使用 `scripts/ganzhi.py` 获取当前干支：

```bash
py -3 scripts/ganzhi.py --json
py -3 scripts/ganzhi.py --json 2026-07-04
# 输出含 日干支、旬空、月建（优先 ephem 精确节气）、当前节气、月将
py -3 scripts/jieqi.py --json 2026-07-04 06:00   # 单独查节气/月建
```

**月建**：`ganzhi.py` 优先调用 `jieqi.py`（ephem 太阳黄经）；无 ephem 时回退近似日期表。

**应期判断规则**：

1. **旬空**：动爻或用神落旬空 → 出旬之日应事（旬空地支出现之日）。如午未空，则午日或未日应事
2. **月破**：某爻地支与月建相冲 → 出月或填实之日应事。如月建巳，亥爻被冲为月破，待亥日或寅月应事
3. **动而逢合**：动爻被日辰或他爻合住 → 待冲开合之日应事
4. **静而逢冲**：静爻被日辰冲动 → 待值日（该地支之日）或逢合之日应事
5. **入墓**：某爻入墓（如木爻入未墓）→ 待冲墓之日（丑日冲未）应事
6. **行速坐迟**（梅花易数）：行路中占 → 应期取其半（如数 10 日则断 5 日）；安坐中占 → 应期加倍；站立中占 → 取原数

> 示例：今日辛卯日，旬空午未。若动爻为午火，午落空亡 → 断「午日出空之时应事」。

### 旬空详解

旬空（空亡）是六爻断卦中极重要的概念。十天干配十二地支，每旬必余二支不配，此二支即为「旬空」。

**旬空表**（按日干支查）：

| 旬首 | 旬空地支 |
|------|----------|
| 甲子旬 | 戌、亥 |
| 甲戌旬 | 申、酉 |
| 甲申旬 | 午、未 |
| 甲午旬 | 辰、巳 |
| 甲辰旬 | 寅、卯 |
| 甲寅旬 | 子、丑 |

**断法**：
- 用神旬空 → 事不落实，心中无底，待出旬（旬空地支值日）应事
- 忌神旬空 → 忌神无力，反而有利
- 世爻旬空 → 自己心虚或诚意不足
- 应爻旬空 → 对方无意或事情落空
- 动爻旬空 → 动不为空，出旬即应；但动而化空则减力
- 静爻旬空 → 真空无力，需待填实

**出空与填实**：
- 出空：旬空地支在日辰上出现之日（如午未空，到午日或未日出空）
- 填实：旬空地支在月建上出现（如午空，月建为午月则填实）

### 暗动

暗动是六爻中一个容易被忽略但影响很大的概念。

**定义**：静爻被日辰冲（六冲关系），称为「暗动」。暗动之爻虽本卦中为静爻，但在实际断卦中应视为有动爻之效。

**判定条件**：
1. 该爻必须是静爻（非老阴/老阳）
2. 该爻地支与当日地支形成六冲关系
3. 该爻不处于旬空状态（旬空被冲为「冲空」，不作暗动论）

**暗动的效力**：
- 暗动之爻可以生克其他爻
- 暗动之爻可以作为用神、忌神的补充力量
- 暗动是「暗中发动」，事情在不知不觉中发生变化
- 暗动持续时间短，通常在当日或短期应验

**与明动的区别**：
- 明动（老阴/老阳）：变化明确，影响持久
- 暗动（日冲静爻）：变化隐蔽，影响短暂

> 示例：今日子日，卦中午火为静爻。子午冲 → 午火暗动。午火为用神则事有转机；午火为忌神则暗中有阻碍。

### 日破

日破与月破类似，但作用于日辰层面。

**定义**：爻的地支与当日地支相冲，且该爻为衰弱状态（休囚无生扶），称为「日破」。

**日破与暗动的区别**：
- 静爻旺相被日冲 → 暗动（有力）
- 静爻衰弱被日冲 → 日破（无力）
- 关键在于爻本身的旺衰状态

**断法**：
- 用神日破 → 事难成，需待出破之日
- 忌神日破 → 忌神无力，反而有利
- 日破之爻逢生扶 → 可解破

### 反吟与伏吟

| 体系 | 判定标准 | 用途 |
|------|----------|------|
| **梅花易数** | 卦名对冲（乾↔坤、坎↔离、震↔巽、艮↔兑）可作辅助象 | 趋势：反复、变动 |
| **六爻纳甲** | **必查地支**：变爻地支与本爻六冲为反吟；变爻地支与本爻相同为伏吟 | 应期与吉凶主断依据 |

**梅花简化**（仅体用/卦象层）：乾变坤、坎变离等对冲卦名 → 反吟象（反复）；本卦变卦同名 → 伏吟象（停滞）。

**六爻正式**（须 `liuyao_pan.py` 装卦后）：逐爻比较纳甲地支冲合。占病反吟主反复；伏吟主拖延。卦名对冲**不能**代替六爻反吟终断。

> 组合：部分爻反吟、部分伏吟 → 先反复后停滞，或反复中夹杂停滞。

### 体用生克

体用定法见上文「体用定法」节。解读时取五行（乾兑金、坤艮土、震巽木、坎水、离火），判生克：体生用→耗泄；用生体→受益；体克用→可成需努力；用克体→困难宜谨慎；比和→顺利。详见 `references/hexagrams.md`。

### 解读框架

输出时应包含以下层次：

1. **卦象总览**：本卦卦名、变卦卦名（如有）、动爻位置。一句话概括卦象的核心意象
2. **卦辞解读**：引用卦辞原文（从 hexagrams.md 或易经 PDF 获取），白话解释，结合问题做针对性解读
3. **动爻分析**（如有动爻）：引用相关爻辞原文，说明动爻含义和影响
4. **体用生克**（可选）：按体用生克简法分析吉凶趋势
5. **万物类象参照**（可选）：根据问卦内容参考 meihua-wanwu-leixiang.md 中的物象表
6. **行止之策**：基于卦象给用户具体建议，什么该做、什么该避
7. **综合判断**：吉凶倾向总结

### 解读原则

- **结合用户具体问题**：卦象要与用户实际处境关联
- **引用原文**：卦辞、爻辞尽量引用原文，再给白话解释
- **有温度**：易经是智慧的指引，不是冷冰冰的吉凶判断。即使卦象不吉，也要指出化解方向
- **不恐吓**：不说「大凶」「必败」等绝对化措辞。易经讲变化趋势，不测必然
- **诚实**：不确定处直接说「此处解读可能存在争议」

### 多体系冲突处理

按**问事类型**定主体系，再叠辅体系（须各跑对应脚本，路径见文首「联占三式」）：

| 问事 | 主体系 | 辅体系 | 辅体系脚本 |
|------|--------|--------|------------|
| 吉凶趋势、要不要做 | **梅花**（本 skill） | 六壬看过程 | `../daliuren-divination/scripts/daliuren.py` |
| 人事因果、谁在动、应期 | **六壬** | 梅花看总调 | 本 skill 起卦脚本 |
| 方位、时机、出行布局 | **奇门** | 梅花看大势 | `../qimen-dunjia/scripts/qimen.py` |
| 财/官/婚/病细节 | **六爻**（本 skill） | 按需 | — |

**呈现顺序**：先报主体系结论 → 辅体系修正过程或行动建议 → 矛盾时说明「主断 / 过程 / 行动」分层，禁止三结果裸堆。

### 文化语境

易经占卜不是玄学迷信，而是一套完整的符号推演系统。理解「为什么用这个方法」比「怎么用」更重要。

**为什么用铜钱法**：铜钱法（三钱法）源自西汉京房，通过随机性引入「天时」因素。三枚铜钱代表天地人三才，六次投掷对应六爻，本质是一个随机数生成器 + 符号映射系统。它不依赖任何超自然力量，依赖的是「随机事件与当下时空状态的对应关系」这一哲学假设。

**为什么看体用生克**：梅花易数的体用理论源自邵雍的「观物」思想——万物皆有体用，体是主体，用是客体。体用生克的本质是「主客关系的五行推演」，不是算命，是关系分析。

**不同流派的差异**：
- **古法六爻**（京房、火珠林）：重纳甲、重世应、重用神，断卦严谨但复杂
- **新法六爻**（现代简化）：重卦象、重体用、重直觉，入门快但深度有限
- **梅花易数**（邵雍）：重卦象、重万物类象、重应期，灵活但需要大量实践
- 本 Skill 以梅花易数为基础，纳甲八宫为进阶补充。用户可根据需求选择深度

**为什么强调「不动不占」**：邵雍的「不动不占，不因事不占」不是故弄玄虚，而是一个认知过滤器——没有具体问题的占卜，解读必然空泛，因为符号系统没有锚点。这跟现代科学中的「假设驱动」是一个逻辑。

---

## 断卦验证机制

**任意 Agent（Cursor / Claude / Hanako 等）只要运行起卦脚本，排盘 JSON 会自动落盘**，不依赖 Agent 是否记得手动写日志。

### 目录结构（每次占卜一个文件夹，自动创建）

```
../卦理日志/records/<YYYYMMDD_NNN>/
  meta.json
  payload.json
  README.md           # 人类可读摘要（自动生成）
  interpretation.json
  feedback.json
  liuyao_pan.json
```

- **新用户**无 `卦理日志` 或 `records` 时，**首次起卦自动创建**，无需手工建目录
- **不上传 git**：`records/` 已在 `.gitignore`

### 自动落盘（脚本内置，默认开启）

| 脚本 | 触发 |
|------|------|
| `meihua_time.py` | 每次成功起卦 |
| `tongqian.py` | 铜钱/数字法 |
| `liuyao_pan.py` | 装卦（可用 `--log-id` 追加到同目录） |
| `daliuren.py` / `qimen.py` | 每次排盘 |

JSON 输出含 `"_session_log": {"id": "...", "dir": "..."}`。**Agent 须读出 id 并告知用户**。

关闭自动日志（仅调试）：环境变量 `DIVINATION_NO_LOG=1`

### Agent 解读后 amend（必做）

排盘脚本只存卦象；**解读完成后**补充问事与倾向：

```bash
py -3 ../卦理日志/divination_log.py amend --id 20260704_001 \
  --question "今日运势如何" \
  --verdict "中性偏吉" \
  --summary "复其初颐其神"
```

若 JSON 中有 `_session_log.id`，用 `--id`；否则 `--latest`。

### 用户事后反馈

```bash
py -3 ../卦理日志/divination_log.py feedback --latest --outcome hit --note "用户原话"
```

`--outcome`：`hit` · `miss` · `partial` · `unknown`

### 六爻链式日志

1. `meihua_time.py --json` → 得 `_session_log.id`
2. `liuyao_pan.py --log-id <id> --json …` → 同目录写入 `liuyao_pan.json`

### 人类可读浏览

每次起卦 / amend / feedback 后，自动生成 Markdown 摘要（**直接打开看，不必读 JSON**）：

| 文件 | 用途 |
|------|------|
| `records/README.md` | **总索引**（在 `../卦理日志/records/` 下） |
| `records/<id>/README.md` | **单次占卜摘要** |

补生成旧记录：`py -3 ../卦理日志/divination_log.py render --all`

### 查询（机器 / CLI）

```bash
py -3 ../卦理日志/divination_log.py list --pending
py -3 ../卦理日志/divination_log.py show --id 20260704_001
```

---

## 输出格式

### 解读前最小摘要（必展示，6 行）

对用户先输出以下摘要，再展开解读（完整 JSON 不必全文粘贴）：

```
1. 起卦法：（meihua_time / tongqian 铜钱 / tongqian 数字）
2. 时间：（公历… → 农历…，若适用）
3. 卦象：本卦… · 变卦… · 动爻… · 互卦…（若适用）
4. 体用：（体… / 用… → 生克关系）— 仅梅花路径
5. 一句话象意：（结合问事）
6. 倾向：（吉/凶/中性/待验）+ 主要依据一行
```

六爻路径可在 4 行改为：世应/用神/旺衰要点。

以 Markdown 输出完整结构：

```
## 🔮 起卦信息
- **起卦方式**：（铜钱法/数字法/梅花易数某法）
- **六爻结果**：（从初爻到上爻，标注老阴/少阳/少阴/老阳）

## ☯ 卦象
**本卦**：第 N 卦 · [卦名]
- 上卦：[卦名]（[符号]） 下卦：[卦名]（[符号]）
- 大象：[大象传原文]

**变卦**：第 N 卦 · [卦名]（如无动爻则省略）
- 动爻：第 N 爻（如有多爻则列出）
- 互卦：第 N 卦 · [卦名]（`meihua_time.py` / `tongqian.py --json` 均含「互卦」）

（可绘制 ASCII 卦象图：阳爻 ████████████  阴爻 ████    ████  动爻标记 ●/○）

---

## 📜 解读
（按解读框架展开）

---

## 🧭 建议
（行止之策）
```

ASCII 卦象绘制要点：

- 阳爻：`████████████`
- 阴爻：`████    ████`
- 动爻标记：`●`（老阳变阴）或 `○`（老阴变阳）
- 六爻从下到上绘制（最下面为初爻）
